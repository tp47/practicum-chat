<div class="text-field">
  <input
    type="{{type}}"
    name="{{name}}"
    id="{{name}}"
    placeholder="{{placeholder}}"
  />
  <label for="{{name}}">{{label}}</label>
  <span class="text-field_error"></span>
</div>
